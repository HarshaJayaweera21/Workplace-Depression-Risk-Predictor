services:

  backend:
    build: ./predictor-api
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./predictor-ui
      args:
        VITE_API_URL: http://localhost:8000
    ports:
      - "80:80"
    depends_on:
      - backend